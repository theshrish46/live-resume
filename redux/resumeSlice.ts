import { createSlice } from '@reduxjs/toolkit';
import type { RootState } from "@/redux/store";
import type {
    ResumeProfile,
    ResumeWorkExperience,
    ResumeEducation,
    ResumeProject,
    ResumeSkills,
    ResumeCustom
} from '@/redux/types';
